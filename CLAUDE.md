# CLAUDE.md

Follow **AGENTS.md** at the repository root. That file is the source of truth
for commands, architecture, tooling, and contribution policy.

Claude-specific:

- Safe commands are pre-approved in `.claude/settings.json`.
- Do not request extra permissions for `npm publish`, `git push`, or merging PRs.
