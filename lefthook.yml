# Local safety net. CI re-runs all of this, so a bypassed hook cannot ship
# anything: these only shorten the feedback loop.
#
# `pre-push` is intentionally absent: lefthook consumes git's stdin to compute
# {push_files}, so a command under it cannot see which refs are being pushed.
# That hook is installed directly by `npm ci` — see scripts/install-git-hooks.mjs.
pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{mts,mjs,js,json}"
      exclude:
        - package-lock.json
      run: npx biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    conventional:
      run: |
        head -n1 {1} | grep -qE '^(feat|fix|chore|ci|docs|test|refactor|perf|style|build|revert)(\([a-z0-9._-]+\))?!?: .+' \
          || { echo 'Commit message must use Conventional Commits (feat:, fix:, chore:, …)'; exit 1; }
